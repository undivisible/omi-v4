name: Release Desktop

on:
  push:
    tags:
      - 'desktop-v*'
  workflow_dispatch:
    inputs:
      version:
        description: Version label used for artifact names on manual runs
        required: false
        default: dev
      dry_run:
        description: Build and upload artifacts without creating a GitHub Release
        type: boolean
        required: false
        default: true

permissions:
  contents: read

concurrency:
  group: release-desktop-${{ github.ref }}
  cancel-in-progress: false

jobs:
  macos:
    name: macOS
    runs-on: macos-15
    outputs:
      version: ${{ steps.meta.outputs.version }}
      dry_run: ${{ steps.meta.outputs.dry_run }}
    steps:
      - uses: actions/checkout@v4

      - id: meta
        env:
          INPUT_VERSION: ${{ inputs.version }}
          INPUT_DRY_RUN: ${{ inputs.dry_run }}
        run: |
          set -euo pipefail
          if [ "${GITHUB_REF_TYPE}" = "tag" ]; then
            version="${GITHUB_REF_NAME#desktop-v}"
            dry_run=false
          else
            version="${INPUT_VERSION:-dev}"
            dry_run="${INPUT_DRY_RUN:-true}"
          fi
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "dry_run=${dry_run}" >> "$GITHUB_OUTPUT"
          echo "RELEASE_VERSION=${version}" >> "$GITHUB_ENV"
          echo "Desktop release version: ${version}"
          if [ "${dry_run}" = "true" ]; then
            echo "::notice::Dry run: artifacts will be uploaded to this workflow run only. No GitHub Release will be created."
          fi

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install the Rust toolchain
        run: |
          set -euo pipefail
          rustup toolchain install stable --profile minimal
          rustup default stable
          rustup target add aarch64-apple-darwin x86_64-apple-darwin
          rustup which --toolchain stable rustc

      - name: Cache pub, CocoaPods and Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
            ~/Library/Caches/CocoaPods
            ~/.cargo/registry
            ~/.cargo/git
            app/native/hub/target
          key: desktop-macos-${{ runner.os }}-${{ hashFiles('app/pubspec.lock', 'app/macos/Podfile.lock', 'app/native/hub/Cargo.toml') }}
          restore-keys: |
            desktop-macos-${{ runner.os }}-

      - name: Decide whether Developer ID signing is possible
        id: signing
        env:
          MACOS_CERTIFICATE_P12_BASE64: ${{ secrets.MACOS_CERTIFICATE_P12_BASE64 }}
          MACOS_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
          MACOS_SIGNING_IDENTITY: ${{ secrets.MACOS_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          set -euo pipefail
          if [ -n "${MACOS_CERTIFICATE_P12_BASE64}" ] && [ -n "${MACOS_CERTIFICATE_PASSWORD}" ] && [ -n "${MACOS_SIGNING_IDENTITY}" ]; then
            echo "sign=true" >> "$GITHUB_OUTPUT"
          else
            echo "sign=false" >> "$GITHUB_OUTPUT"
            echo "::notice::MACOS_CERTIFICATE_P12_BASE64 / MACOS_CERTIFICATE_PASSWORD / MACOS_SIGNING_IDENTITY are not all set. The app will be ad-hoc signed only; Gatekeeper will require a right-click Open on first launch."
          fi
          if [ -n "${APPLE_ID}" ] && [ -n "${APPLE_APP_SPECIFIC_PASSWORD}" ] && [ -n "${APPLE_TEAM_ID}" ] && [ -n "${MACOS_SIGNING_IDENTITY}" ]; then
            echo "notarize=true" >> "$GITHUB_OUTPUT"
          else
            echo "notarize=false" >> "$GITHUB_OUTPUT"
            echo "::notice::Notarization secrets are incomplete; skipping notarization and stapling."
          fi

      - name: Import the Developer ID certificate
        if: steps.signing.outputs.sign == 'true'
        env:
          MACOS_CERTIFICATE_P12_BASE64: ${{ secrets.MACOS_CERTIFICATE_P12_BASE64 }}
          MACOS_CERTIFICATE_PASSWORD: ${{ secrets.MACOS_CERTIFICATE_PASSWORD }}
        run: |
          set -euo pipefail
          keychain="${RUNNER_TEMP}/omi-desktop.keychain-db"
          keychain_password="$(uuidgen)"
          security create-keychain -p "${keychain_password}" "${keychain}"
          security set-keychain-settings -lut 21600 "${keychain}"
          security unlock-keychain -p "${keychain_password}" "${keychain}"
          cert="${RUNNER_TEMP}/macos-certificate.p12"
          printf '%s' "${MACOS_CERTIFICATE_P12_BASE64}" | base64 -d > "${cert}"
          security import "${cert}" -P "${MACOS_CERTIFICATE_PASSWORD}" -A -t cert -f pkcs12 -k "${keychain}"
          security set-key-partition-list -S apple-tool:,apple:,codesign: -k "${keychain_password}" "${keychain}" >/dev/null
          security list-keychain -d user -s "${keychain}" login.keychain-db
          rm -f "${cert}"
          security find-identity -v -p codesigning "${keychain}"

      - name: Build the macOS release app
        working-directory: app
        run: |
          set -euo pipefail
          mkdir -p build/macos/SourcePackages
          flutter pub get
          flutter build macos --release

      - name: Locate the built app bundle
        id: bundle
        run: |
          set -euo pipefail
          app="$(find app/build/macos/Build/Products/Release -maxdepth 1 -name '*.app' | head -n1 || true)"
          if [ -z "${app}" ]; then
            echo "::error::No .app bundle was produced under app/build/macos/Build/Products/Release."
            exit 1
          fi
          echo "path=${app}" >> "$GITHUB_OUTPUT"
          echo "APP_BUNDLE=${app}" >> "$GITHUB_ENV"
          echo "Built ${app}"

      - name: Sign the app bundle
        env:
          MACOS_SIGNING_IDENTITY: ${{ secrets.MACOS_SIGNING_IDENTITY }}
        run: |
          set -euo pipefail
          app="${APP_BUNDLE}"
          if [ "${{ steps.signing.outputs.sign }}" = "true" ]; then
            codesign --force --deep --timestamp --options runtime \
              --entitlements app/macos/Runner/Release.entitlements \
              --sign "${MACOS_SIGNING_IDENTITY}" "${app}"
          else
            codesign --force --deep --sign - "${app}"
          fi
          codesign --verify --deep --strict --verbose=2 "${app}"

      - name: Package the app as a zip
        run: |
          set -euo pipefail
          mkdir -p artifacts
          ditto -c -k --sequesterRsrc --keepParent \
            "${APP_BUNDLE}" \
            "artifacts/omi-${RELEASE_VERSION}-macos.zip"

      - name: Notarize and staple the app
        if: steps.signing.outputs.notarize == 'true'
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          set -euo pipefail
          zip="artifacts/omi-${RELEASE_VERSION}-macos.zip"
          xcrun notarytool submit "${zip}" \
            --apple-id "${APPLE_ID}" \
            --password "${APPLE_APP_SPECIFIC_PASSWORD}" \
            --team-id "${APPLE_TEAM_ID}" \
            --wait
          xcrun stapler staple "${APP_BUNDLE}"
          rm -f "${zip}"
          ditto -c -k --sequesterRsrc --keepParent \
            "${APP_BUNDLE}" "${zip}"

      - name: Build the DMG
        run: |
          set -euo pipefail
          staging="${RUNNER_TEMP}/dmg"
          rm -rf "${staging}"
          mkdir -p "${staging}"
          cp -R "${APP_BUNDLE}" "${staging}/"
          ln -s /Applications "${staging}/Applications"
          hdiutil create -volname "Omi" -srcfolder "${staging}" -ov -format UDZO \
            "artifacts/omi-${RELEASE_VERSION}-macos.dmg"

      - name: Sign, notarize and staple the DMG
        if: steps.signing.outputs.notarize == 'true'
        env:
          MACOS_SIGNING_IDENTITY: ${{ secrets.MACOS_SIGNING_IDENTITY }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          set -euo pipefail
          dmg="artifacts/omi-${RELEASE_VERSION}-macos.dmg"
          codesign --force --timestamp --sign "${MACOS_SIGNING_IDENTITY}" "${dmg}"
          xcrun notarytool submit "${dmg}" \
            --apple-id "${APPLE_ID}" \
            --password "${APPLE_APP_SPECIFIC_PASSWORD}" \
            --team-id "${APPLE_TEAM_ID}" \
            --wait
          xcrun stapler staple "${dmg}"

      - name: Label unsigned artifacts
        if: steps.signing.outputs.sign != 'true'
        run: |
          set -euo pipefail
          version="${RELEASE_VERSION}"
          mv "artifacts/omi-${version}-macos.zip" "artifacts/omi-${version}-macos-unsigned.zip"
          mv "artifacts/omi-${version}-macos.dmg" "artifacts/omi-${version}-macos-unsigned.dmg"
          ls -l artifacts

      - uses: actions/upload-artifact@v4
        with:
          name: desktop-macos
          path: artifacts/*
          if-no-files-found: error
          retention-days: 14

  bundle:
    name: Bundle desktop artifacts
    runs-on: ubuntu-24.04
    needs: macos
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist
          pattern: desktop-*
          merge-multiple: true

      - name: Checksum the artifacts
        run: |
          set -euo pipefail
          cd dist
          sha256sum -- * > SHA256SUMS.txt
          cat SHA256SUMS.txt

      - uses: actions/upload-artifact@v4
        with:
          name: desktop-release-bundle
          path: dist/*
          if-no-files-found: error
          retention-days: 14

      - name: Summarise the run
        env:
          DRY_RUN: ${{ needs.macos.outputs.dry_run }}
          RELEASE_VERSION: ${{ needs.macos.outputs.version }}
        run: |
          set -euo pipefail
          {
            echo "### Desktop ${RELEASE_VERSION}"
            echo
            if [ "${DRY_RUN}" = "true" ]; then
              echo "- Dry run: no GitHub Release was created. Download \`desktop-release-bundle\` from this run."
            else
              echo "- Publishing a GitHub Release for \`${GITHUB_REF_NAME}\`."
            fi
          } >> "$GITHUB_STEP_SUMMARY"

  publish:
    name: Publish desktop release
    runs-on: ubuntu-24.04
    needs: [macos, bundle]
    if: needs.macos.outputs.dry_run == 'false'
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: desktop-release-bundle
          path: dist

      - name: Create the GitHub release
        uses: softprops/action-gh-release@v2
        with:
          name: Desktop ${{ needs.macos.outputs.version }}
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          fail_on_unmatched_files: true
          files: dist/*
